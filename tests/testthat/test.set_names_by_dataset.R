context('set_names_by_dataset')


#test_that('get colors by artifical dataset',{
  
  # make dataset
  #df <- read_input("data/test.BCL2vsIgG.GPiN.txt",header=T,sep="\t")$data
  #df <- calc_mod_ttest(df)
  #df <- id_enriched_proteins(df)
  #df$dataset = 'pulldown'
  
  # get overlay dataset
  
  #overlay = get_inweb_list('BCL2')
  #overlay = overlay[overlay$significant, ]
  #overlay$dataset = 'inweb'
  #overlay = to_overlay_data(overlay)
  #overlay$color = ifelse(overlay$significant, )
  
  #global_colors = set_names_by_dataset(df, overlay)
  
  # check output
  #expect_true(all(names(global_colors) == c("B", "A", "A", "B")))
  #expect_true(all(as.vector(global_colors) == c("blue", "red", "red", "blue")))
#})